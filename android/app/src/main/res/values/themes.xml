<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 基础主题，用于 AndroidManifest，让应用窗口有一个基本的样式 -->
    <style name="Theme.MySelfManagementApp" parent="android:Theme.Material.Light.NoActionBar">
        <!-- 可选：设置状态栏颜色等，但 Compose 会覆盖大部分 UI，所以简单继承即可 -->
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>